---
import { Produtes } from "@/components/product.tsx";
import OtherPageLayout from "@/layouts/OtherPageLayout.astro";
---

<OtherPageLayout>
  <!-- Hero Section -->
  <section
    class="relative bg-cover bg-center h-[400px] flex items-center"
    style="background-image: url('/bgproduct.avif');"
  >
    <div class="absolute inset-0 bg-black bg-opacity-60"></div>

    <div
      class="relative max-w-7xl mx-auto flex flex-col justify-center h-full px-6 sm:px-10 lg:px-12"
    >
      <div data-aos-delay="300">
        <p
          class="text-[#FF8F00] font-semibold text-lg sm:text-xl tracking-wide mb-3"
          data-aos="fade-up"
        >
          Our Premium Products
        </p>
        <h1
          class="text-white text-3xl sm:text-4xl lg:text-5xl font-extrabold leading-tight"
          data-aos="fade-up"
          data-aos-delay="100"
        >
          Explore Our High-Quality Rice Varieties
        </h1>
      </div>
    </div>
  </section>
  <!-- Description -->
  <section class="relative -translate-y-1/2 z-20 px-4 sm:px-6">
    <!-- Right Navigation Section -->

    <div
      class="bg-[#F5F5DC] max-w-6xl mx-auto py-2 sm:py-6 px-6 sm:px-10 lg:px-12 rounded-full border border-[#4B4B4B] shadow-lg"
      data-aos="fade-up"
      data-aos-delay="300"
    >
      <p
        class="text-center text-gray-700 text-xs sm:text-base md:text-lg leading-relaxed"
      >
        Our commitment to excellence starts from the fields and is perfected in
        our mills, ensuring superior quality in every grain.
      </p>
    </div>
  </section>

  <div class="p-4"></div>
  <section class="py-12">
    <div class="border rounded-lg max-w-7xl mx-auto">
      <Produtes client:load />
    </div>
  </section>
</OtherPageLayout>
